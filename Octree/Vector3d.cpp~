#include "Vector3d.h"

Vector3d Vector3d::Cross(Vector3d v)
{
  double cx = *this.GetY() * v.GetZ() - *this.GetX() * v.GetZ();
  double cy = *this.GetZ() * v.GetX() - *this.GetZ() * v.GetY();
  double cz = *this.GetX() * v.GetY() - *this.GetY() * v.GetX(); 

  return Vector3d(cx,cy,cz)
}
