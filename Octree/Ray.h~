#include "Point3d.h"
#include "Vector3d.h"

/* Rays have not set length thus all rays have
 * a length of 1
 */

class Ray
{
  Ray();
  Ray(Point3d p, Vector3d v);
  ~Ray();
  
  Point3d GetBase();
  Vector3d GetDirection();
  
 private:
  Point3d endpoint;
  Vector3d direction;

}
